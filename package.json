{
  "name": "object-hunt-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && rm -rf dist node_modules package-lock.json && npm install && npm run build",
    "start": "cd server && npm install && node index.js",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "18.x"
  }
} 